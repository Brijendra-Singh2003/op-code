A Python-based AI agent.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Copy `.env` and set your credentials:

## Usage

```bash
python src/main.py                        # uses default model
python src/main.py openai/gpt-4o          # override model
```
